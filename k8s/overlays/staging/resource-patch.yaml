apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-api
spec:
  template:
    spec:
      containers:
        - name: flask-api
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
